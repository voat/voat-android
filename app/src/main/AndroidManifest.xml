<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.voat.android" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".VoatApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SettingsActivity"/>
        <activity android:name=".SubmissionActivity"/>
        <activity android:name=".UserActivity"/>
        <activity android:name=".SubscriptionsActivity"/>
        <activity android:name=".MessagesActivity"/>
        <activity android:name=".ImageActivity" android:theme="@style/AppTheme.TranslucentWindow"/>
    </application>

</manifest>
